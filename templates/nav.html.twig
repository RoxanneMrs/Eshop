{# accueil, créations, contact/commande perso, livre d'or, à propos #}

{# BURGER MENU #}
  <div id="burger_menu">
    <div>---</div>
    <div>---</div>
    <div>---</div>
  </div>


<nav id="nav">

  <ul class="d-flex">
  
    <li>
      <a href="{{ path('app_home') }}"> Accueil </a>
    </li>

    <li id="creations">
     
        <a href="{{ path('app_product_index') }}" id="creations" class="dropdown-toggle"> Nos créations </a>
        {# <span id="dropdown_toggle">V</span> #}
   
      <ul id="dropdown">
          <li> 
            <a href="{{ path('app_product_index') }}"> Voir tout </a>
          </li>
          {% for category in categories %}
          <li>
            <a href="{{ path('app_get_product_by_category', {id_category : category.id}) }}" id="nav-dropdown" data-category-id="{{ category.id }}"> {{category.title}} </a>
          </li>
          {% endfor %}
      </ul>
    </li>

    <li>
      <a href="{{ path('app_contact') }}"> Contact </a>
    </li>

    <li>
      <a href="{{path('app_comment')}}" > Livre d'or </a>
    </li>

    {% if is_granted('ROLE_USER') %}
      <li>
        <a href="{{ path('app_profile') }}"> Profil </a>
      </li>

      <li>
        <a href="{{ path('app_logout') }}"> Déconnexion </a>
      </li>

    {% else %}
      <li>
        <a href="{{ path('app_login') }}"> Connexion </a>
      </li>

      <li>
        <a href="{{ path('app_register') }}"> S'inscrire </a>
      </li>
    {% endif %}
  </ul>

  
</nav>